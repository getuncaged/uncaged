# Uncaged Tab Config
# Stored in ~/.uncaged/tab_configs/ — rename this file and edit anytime!
#
# Tip: Ask Uncaged to update this tab config for you using /skills -> /update-tab-config!
#   e.g. "/update-tab-config turn this into a 2x2 tab config with one pane running my dev server"
#
# Docs at https://github.com/uncaged-terminal/uncaged

name = "My Tab Config"

[[panes]]
id = "main"
type = "terminal"
# directory = "~/code/my-project"
commands = []
